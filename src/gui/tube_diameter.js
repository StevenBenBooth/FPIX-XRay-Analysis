const resultField = document.querySelector("textarea");

function updateImage() {
  eel.update_img(resultField.value)
}  // Should be able to write (eel.update_img) or just (update_img) here instead...